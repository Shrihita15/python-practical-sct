Question 1: Railway Reservation Management System

CREATE TYPE ADDRESS_TYPE AS OBJECT(
STREET VARCHAR2(50),
CITY VARCHAR2(50),
PINCODE VARCHAR2(10));

CREATE TABLE PASSENGER(
PASSENGER_ID INT PRIMARY KEY,
NAME VARCHAR2(50),
PHONE_NO VARCHAR2(15),
ADDRESS ADDRESS_TYPE
);

CREATE TABLE TRAIN(
TRAIN_NO INT PRIMARY KEY,
TRAIN_NAME VARCHAR2(50),
SOURCE VARCHAR2(30),
DESTINATION VARCHAR2(30)
);

CREATE TABLE RESERVATION(
PNR_NO INT PRIMARY KEY,
PASSENGER_ID INT REFERENCES PASSENGER(PASSENGER_ID),
TRAIN_NO INT REFERENCES TRAIN(TRAIN_NO),
SEAT_NO INT,
STATUS VARCHAR2(20)
);

INSERT INTO PASSENGER VALUES(101,'RAHUL SHARMA','9876543210',ADDRESS_TYPE('M.G  ROAD','MUMBAI','40001'));

INSERT INTO PASSENGER VALUES(102,'ANUSHKA SEN','9876543211',ADDRESS_TYPE('PARK STREET','KOLKATA','700016'));

INSERT INTO PASSENGER VALUES(103,'SALMAN KHAN','9876543212',ADDRESS_TYPE('F.C  ROAD','PUNE','411004'));

INSERT INTO PASSENGER VALUES(104,'AMIR KHAN','9876543213',ADDRESS_TYPE('SAROJINI NAGAR','DELHI','110001'));

INSERT INTO PASSENGER VALUES(105,'SAMAIRA KHANNA','9876543215',ADDRESS_TYPE('ANNA NAGAR','CHENNAI','600040'));

INSERT INTO Train VALUES (12951, 'Rajdhani Express', 'Mumbai', 'Delhi');
INSERT INTO Train VALUES (12123, 'Deccan Queen', 'Pune', 'Mumbai');
INSERT INTO Train VALUES (12261, 'Duronto Express', 'Mumbai', 'Kolkata');
INSERT INTO Train VALUES (12626, 'Kerala Express', 'Delhi', 'Trivandrum');
INSERT INTO Train VALUES (12002, 'Shatabdi Express', 'New Delhi', 'Bhopal');

INSERT INTO Reservation VALUES (10001, 101, 12951, 12, 'Confirmed');
INSERT INTO Reservation VALUES (10002, 102, 12951, 14, 'Confirmed');
INSERT INTO Reservation VALUES (10003, 103, 12123, 5, 'Confirmed');
INSERT INTO Reservation VALUES (10004, 104, 12951, 15, 'Confirmed');
INSERT INTO Reservation VALUES (10005, 105, 12261, 22, 'Waiting');

1.	Display passenger name and city.
SELECT NAME, P.ADDRESS.CITY AS CITY FROM PASSENGER P

2.	Display all passengers travelling to Mumbai.
SELECT DISTINCT P.NAME ,T.DESTINATION
FROM PASSENGER P
JOIN RESERVATION R ON P.PASSENGER_ID = R.PASSENGER_ID
JOIN TRAIN T ON R.TRAIN_NO = T.TRAIN_NO
WHERE T.DESTINATION = 'Mumbai';

3.	Display train-wise reservation count.

SELECT TRAIN_NO,COUNT(*) AS TOTAL_RESERVATION
FROM RESERVATION
GROUP BY TRAIN_NO;

4.	Display the train having the highest number of reservations.

SELECT *
FROM (
    SELECT TRAIN_NO, COUNT(*) AS TOTAL_RESERVATION
    FROM RESERVATION
    GROUP BY TRAIN_NO
    ORDER BY TOTAL_RESERVATION DESC
)
WHERE ROWNUM = 1;

5.	Create a backup table of Reservation.

CREATE TABLE Reservation_Backup AS 
SELECT * FROM Reservation;

## Question 2: Insurance Claim Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE CUSTOMER(
  CUSTOMER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  PHONE_NO VARCHAR2(15),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE POLICY(
  POLICY_NO INT PRIMARY KEY,
  POLICY_TYPE VARCHAR2(30),
  PREMIUM NUMBER,
  CUSTOMER_ID INT REFERENCES CUSTOMER(CUSTOMER_ID)
);

CREATE TABLE CLAIM(
  CLAIM_ID INT PRIMARY KEY,
  POLICY_NO INT REFERENCES POLICY(POLICY_NO),
  CLAIM_AMOUNT NUMBER,
  CLAIM_STATUS VARCHAR2(20)
);

INSERT INTO CUSTOMER VALUES(201,'RAHUL SHARMA','9876543210',ADDRESS_TYPE('M.G ROAD','MUMBAI','400001'));
INSERT INTO CUSTOMER VALUES(202,'ANUSHKA SEN','9876543211',ADDRESS_TYPE('PARK STREET','KOLKATA','700016'));
INSERT INTO CUSTOMER VALUES(203,'SALMAN KHAN','9876543212',ADDRESS_TYPE('F.C ROAD','PUNE','411004'));
INSERT INTO CUSTOMER VALUES(204,'AMIR KHAN','9876543213',ADDRESS_TYPE('SAROJINI NAGAR','DELHI','110001'));
INSERT INTO CUSTOMER VALUES(205,'SAMAIRA KHANNA','9876543215',ADDRESS_TYPE('ANNA NAGAR','CHENNAI','600040'));

INSERT INTO POLICY VALUES(301,'Health',15000,201);
INSERT INTO POLICY VALUES(302,'Life',25000,202);
INSERT INTO POLICY VALUES(303,'Vehicle',8000,203);
INSERT INTO POLICY VALUES(304,'Health',18000,204);
INSERT INTO POLICY VALUES(305,'Life',30000,205);

INSERT INTO CLAIM VALUES(401,301,50000,'Approved');
INSERT INTO CLAIM VALUES(402,302,200000,'Approved');
INSERT INTO CLAIM VALUES(403,303,15000,'Pending');
INSERT INTO CLAIM VALUES(404,304,40000,'Approved');
INSERT INTO CLAIM VALUES(405,305,150000,'Rejected');

-- Part B
-- 1. Customer name and city
SELECT NAME, C.ADDRESS.CITY AS CITY FROM CUSTOMER C;

-- 2. Approved claims
SELECT * FROM CLAIM WHERE CLAIM_STATUS = 'Approved';

-- 3. Total claim amount policy-wise
SELECT POLICY_NO, SUM(CLAIM_AMOUNT) AS TOTAL_CLAIM
FROM CLAIM GROUP BY POLICY_NO;

-- 4. Customer with highest claim amount
SELECT * FROM (
  SELECT C.NAME, CL.CLAIM_AMOUNT
  FROM CUSTOMER C
  JOIN POLICY P ON C.CUSTOMER_ID = P.CUSTOMER_ID
  JOIN CLAIM CL ON P.POLICY_NO = CL.POLICY_NO
  ORDER BY CL.CLAIM_AMOUNT DESC
) WHERE ROWNUM = 1;

-- 5. Backup table
CREATE TABLE CLAIM_BACKUP AS SELECT * FROM CLAIM;

-- 6. Explain (theory):
-- Replicated customer databases let a regional office keep operating and settling claims
even if the main/regional server goes down, since a synchronized copy is available at
-- another site. This is essential for business continuity in disaster recovery scenarios.
```

---

## Question 3: Courier and Logistics Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE CUSTOMER(
  CUSTOMER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  PHONE_NO VARCHAR2(15),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE PARCEL(
  PARCEL_ID INT PRIMARY KEY,
  SENDER_ID INT REFERENCES CUSTOMER(CUSTOMER_ID),
  RECEIVER_NAME VARCHAR2(50),
  WEIGHT NUMBER
);

CREATE TABLE DELIVERY(
  DELIVERY_ID INT PRIMARY KEY,
  PARCEL_ID INT REFERENCES PARCEL(PARCEL_ID),
  BRANCH VARCHAR2(30),
  STATUS VARCHAR2(20)
);

INSERT INTO CUSTOMER VALUES(1,'RAHUL SHARMA','9876543210',ADDRESS_TYPE('M.G ROAD','MUMBAI','400001'));
INSERT INTO CUSTOMER VALUES(2,'ANUSHKA SEN','9876543211',ADDRESS_TYPE('PARK STREET','KOLKATA','700016'));
INSERT INTO CUSTOMER VALUES(3,'SALMAN KHAN','9876543212',ADDRESS_TYPE('F.C ROAD','PUNE','411004'));
INSERT INTO CUSTOMER VALUES(4,'AMIR KHAN','9876543213',ADDRESS_TYPE('SAROJINI NAGAR','DELHI','110001'));
INSERT INTO CUSTOMER VALUES(5,'SAMAIRA KHANNA','9876543215',ADDRESS_TYPE('ANNA NAGAR','CHENNAI','600040'));

INSERT INTO PARCEL VALUES(101,1,'Vikram Rao',2.5);
INSERT INTO PARCEL VALUES(102,2,'Neha Joshi',1.2);
INSERT INTO PARCEL VALUES(103,3,'Karan Mehta',5.0);
INSERT INTO PARCEL VALUES(104,4,'Divya Nair',0.8);
INSERT INTO PARCEL VALUES(105,5,'Arjun Singh',3.4);

INSERT INTO DELIVERY VALUES(1001,101,'Mumbai Hub','Delivered');
INSERT INTO DELIVERY VALUES(1002,102,'Kolkata Hub','Delivered');
INSERT INTO DELIVERY VALUES(1003,103,'Pune Hub','In Transit');
INSERT INTO DELIVERY VALUES(1004,104,'Delhi Hub','Delivered');
INSERT INTO DELIVERY VALUES(1005,105,'Chennai Hub','Pending');

-- Part B
-- 1. Sender name and city
SELECT NAME, C.ADDRESS.CITY AS CITY FROM CUSTOMER C;

-- 2. Parcels delivered successfully
SELECT P.PARCEL_ID, D.STATUS FROM PARCEL P
JOIN DELIVERY D ON P.PARCEL_ID = D.PARCEL_ID
WHERE D.STATUS = 'Delivered';

-- 3. Branch-wise parcel count
SELECT BRANCH, COUNT(*) AS TOTAL_PARCELS
FROM DELIVERY GROUP BY BRANCH;

-- 4. Customer having maximum shipments
SELECT * FROM (
  SELECT C.NAME, COUNT(*) AS SHIPMENTS
  FROM CUSTOMER C JOIN PARCEL P ON C.CUSTOMER_ID = P.SENDER_ID
  GROUP BY C.NAME
  ORDER BY SHIPMENTS DESC
) WHERE ROWNUM = 1;

-- 5. Backup table
CREATE TABLE PARCEL_BACKUP AS SELECT * FROM PARCEL;

-- 6. Explain (theory):
-- Replicating parcel data between regional servers lets any branch office look up a
-- parcel's status without depending on a single central server, improving tracking
-- speed and availability even if one warehouse server is temporarily unreachable.
```

---

## Question 4: School Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE TEACHER(
  TEACHER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  SUBJECT VARCHAR2(30)
);

CREATE TABLE CLASS(
  CLASS_ID INT PRIMARY KEY,
  CLASS_NAME VARCHAR2(20),
  TEACHER_ID INT REFERENCES TEACHER(TEACHER_ID)
);

CREATE TABLE STUDENT(
  STUDENT_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE,
  CLASS_ID INT REFERENCES CLASS(CLASS_ID),
  PERCENTAGE NUMBER
);

INSERT INTO TEACHER VALUES(1,'Mrs. Iyer','Mathematics');
INSERT INTO TEACHER VALUES(2,'Mr. Kapoor','Science');
INSERT INTO TEACHER VALUES(3,'Ms. Das','English');

INSERT INTO CLASS VALUES(10,'10-A',1);
INSERT INTO CLASS VALUES(20,'10-B',2);
INSERT INTO CLASS VALUES(30,'9-A',3);

INSERT INTO STUDENT VALUES(101,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'),10,82);
INSERT INTO STUDENT VALUES(102,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'),10,68);
INSERT INTO STUDENT VALUES(103,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'),20,91);
INSERT INTO STUDENT VALUES(104,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'),20,74);
INSERT INTO STUDENT VALUES(105,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'),30,88);

-- Part B
-- 1. Student name and city
SELECT NAME, S.ADDRESS.CITY AS CITY FROM STUDENT S;

-- 2. Teacher-wise student count
SELECT T.NAME, COUNT(S.STUDENT_ID) AS STUDENT_COUNT
FROM TEACHER T
JOIN CLASS C ON T.TEACHER_ID = C.TEACHER_ID
JOIN STUDENT S ON C.CLASS_ID = S.CLASS_ID
GROUP BY T.NAME;

-- 3. Students scoring above 75%
SELECT NAME, PERCENTAGE FROM STUDENT WHERE PERCENTAGE > 75;

-- 4. Class having maximum students
SELECT * FROM (
  SELECT C.CLASS_NAME, COUNT(*) AS TOTAL_STUDENTS
  FROM CLASS C JOIN STUDENT S ON C.CLASS_ID = S.CLASS_ID
  GROUP BY C.CLASS_NAME
  ORDER BY TOTAL_STUDENTS DESC
) WHERE ROWNUM = 1;

-- 5. Backup table
CREATE TABLE STUDENT_BACKUP AS SELECT * FROM STUDENT;

-- 6. Explain (theory):
-- Parallel processing splits the task of computing marks, ranks and grades for
-- thousands of students across multiple processors simultaneously, so report cards
-- for an entire campus can be generated in a fraction of the time a single-threaded
-- process would take.
```

---

## Question 5: Pharmacy Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE SUPPLIER(
  SUPPLIER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  PHONE_NO VARCHAR2(15),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE MEDICINE(
  MEDICINE_ID INT PRIMARY KEY,
  MEDICINE_NAME VARCHAR2(50),
  STOCK INT,
  PRICE NUMBER,
  SUPPLIER_ID INT REFERENCES SUPPLIER(SUPPLIER_ID)
);

CREATE TABLE SALES(
  SALE_ID INT PRIMARY KEY,
  MEDICINE_ID INT REFERENCES MEDICINE(MEDICINE_ID),
  QUANTITY INT,
  AMOUNT NUMBER
);

INSERT INTO SUPPLIER VALUES(1,'Cipla Distributors','9876543210',ADDRESS_TYPE('M.G Road','Mumbai','400001'));
INSERT INTO SUPPLIER VALUES(2,'Sun Pharma Agency','9876543211',ADDRESS_TYPE('Park Street','Kolkata','700016'));
INSERT INTO SUPPLIER VALUES(3,'Zydus Traders','9876543212',ADDRESS_TYPE('F.C Road','Pune','411004'));
INSERT INTO SUPPLIER VALUES(4,'Mankind Supplies','9876543213',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'));
INSERT INTO SUPPLIER VALUES(5,'Cadila Traders','9876543215',ADDRESS_TYPE('Anna Nagar','Chennai','600040'));

INSERT INTO MEDICINE VALUES(101,'Paracetamol',15,20,1);
INSERT INTO MEDICINE VALUES(102,'Amoxicillin',50,80,2);
INSERT INTO MEDICINE VALUES(103,'Cetrizine',8,15,3);
INSERT INTO MEDICINE VALUES(104,'Metformin',100,45,4);
INSERT INTO MEDICINE VALUES(105,'Ibuprofen',18,30,5);

INSERT INTO SALES VALUES(1,101,30,600);
INSERT INTO SALES VALUES(2,102,60,4800);
INSERT INTO SALES VALUES(3,103,10,150);
INSERT INTO SALES VALUES(4,104,120,5400);
INSERT INTO SALES VALUES(5,105,25,750);

-- Part B
-- 1. Supplier name and city
SELECT NAME, S.ADDRESS.CITY AS CITY FROM SUPPLIER S;

-- 2. Medicines with stock less than 20
SELECT MEDICINE_NAME, STOCK FROM MEDICINE WHERE STOCK < 20;

-- 3. Medicine having highest sales
SELECT * FROM (
  SELECT M.MEDICINE_NAME, SUM(S.AMOUNT) AS TOTAL_SALES
  FROM MEDICINE M JOIN SALES S ON M.MEDICINE_ID = S.MEDICINE_ID
  GROUP BY M.MEDICINE_NAME
  ORDER BY TOTAL_SALES DESC
) WHERE ROWNUM = 1;

-- 4. Total sales amount
SELECT SUM(AMOUNT) AS TOTAL_SALES_AMOUNT FROM SALES;

-- 5. Backup table
CREATE TABLE MEDICINE_BACKUP AS SELECT * FROM MEDICINE;

-- 6. Explain (theory):
-- Replicating inventory data to a central warehouse lets management track stock
-- levels across all branches in real time and reorder before a branch runs out,
-- even if a branch's local server is temporarily down.
```

---

## Question 6: Manufacturing Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE FACTORY(
  FACTORY_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE PRODUCT(
  PRODUCT_ID INT PRIMARY KEY,
  PRODUCT_NAME VARCHAR2(50),
  FACTORY_ID INT REFERENCES FACTORY(FACTORY_ID)
);

CREATE TABLE PRODUCTION(
  PRODUCTION_ID INT PRIMARY KEY,
  PRODUCT_ID INT REFERENCES PRODUCT(PRODUCT_ID),
  QUANTITY INT,
  PRODUCTION_DATE DATE
);

INSERT INTO FACTORY VALUES(1,'Tata Plant',ADDRESS_TYPE('M.G Road','Mumbai','400001'));
INSERT INTO FACTORY VALUES(2,'Bajaj Unit',ADDRESS_TYPE('Park Street','Pune','411004'));
INSERT INTO FACTORY VALUES(3,'Godrej Unit',ADDRESS_TYPE('F.C Road','Kolkata','700016'));
INSERT INTO FACTORY VALUES(4,'Mahindra Plant',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'));
INSERT INTO FACTORY VALUES(5,'L&T Unit',ADDRESS_TYPE('Anna Nagar','Chennai','600040'));

INSERT INTO PRODUCT VALUES(101,'Engine Block',1);
INSERT INTO PRODUCT VALUES(102,'Gear Box',2);
INSERT INTO PRODUCT VALUES(103,'Refrigerator',3);
INSERT INTO PRODUCT VALUES(104,'Tractor Wheel',4);
INSERT INTO PRODUCT VALUES(105,'Steel Beam',5);

INSERT INTO PRODUCTION VALUES(1,101,500,DATE '2025-01-10');
INSERT INTO PRODUCTION VALUES(2,102,800,DATE '2025-02-15');
INSERT INTO PRODUCTION VALUES(3,103,650,DATE '2025-03-05');
INSERT INTO PRODUCTION VALUES(4,104,900,DATE '2025-04-20');
INSERT INTO PRODUCTION VALUES(5,105,300,DATE '2025-05-01');

-- Part B
-- 1. Factory name and city
SELECT NAME, F.ADDRESS.CITY AS CITY FROM FACTORY F;

-- 2. Products manufactured in Mumbai
SELECT P.PRODUCT_NAME
FROM PRODUCT P JOIN FACTORY F ON P.FACTORY_ID = F.FACTORY_ID
WHERE F.ADDRESS.CITY = 'Mumbai';

-- 3. Factory-wise production quantity
SELECT F.NAME, SUM(PR.QUANTITY) AS TOTAL_QTY
FROM FACTORY F
JOIN PRODUCT P ON F.FACTORY_ID = P.FACTORY_ID
JOIN PRODUCTION PR ON P.PRODUCT_ID = PR.PRODUCT_ID
GROUP BY F.NAME;

-- 4. Most produced product
SELECT * FROM (
  SELECT P.PRODUCT_NAME, SUM(PR.QUANTITY) AS TOTAL_QTY
  FROM PRODUCT P JOIN PRODUCTION PR ON P.PRODUCT_ID = PR.PRODUCT_ID
  GROUP BY P.PRODUCT_NAME
  ORDER BY TOTAL_QTY DESC
) WHERE ROWNUM = 1;

-- 5. Backup table
CREATE TABLE PRODUCT_BACKUP AS SELECT * FROM PRODUCT;

-- 6. Explain (theory):
-- Parallel processing divides production reporting across multiple CPUs so
-- aggregation over millions of production records from different factories
-- can be computed concurrently, drastically cutting report generation time.
```

---

## Question 7: Food Delivery Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE CUSTOMER(
  CUSTOMER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE RESTAURANT(
  RESTAURANT_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  CITY VARCHAR2(30)
);

CREATE TABLE ORDERS(
  ORDER_ID INT PRIMARY KEY,
  CUSTOMER_ID INT REFERENCES CUSTOMER(CUSTOMER_ID),
  RESTAURANT_ID INT REFERENCES RESTAURANT(RESTAURANT_ID),
  AMOUNT NUMBER
);

INSERT INTO CUSTOMER VALUES(1,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'));
INSERT INTO CUSTOMER VALUES(2,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'));
INSERT INTO CUSTOMER VALUES(3,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'));
INSERT INTO CUSTOMER VALUES(4,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'));
INSERT INTO CUSTOMER VALUES(5,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'));

INSERT INTO RESTAURANT VALUES(101,'Spice Villa','Mumbai');
INSERT INTO RESTAURANT VALUES(102,'Bengal Bites','Kolkata');
INSERT INTO RESTAURANT VALUES(103,'Punjabi Tadka','Pune');
INSERT INTO RESTAURANT VALUES(104,'Delhi Darbar','Delhi');
INSERT INTO RESTAURANT VALUES(105,'South Spice','Chennai');

INSERT INTO ORDERS VALUES(1,1,101,450);
INSERT INTO ORDERS VALUES(2,2,102,300);
INSERT INTO ORDERS VALUES(3,3,103,600);
INSERT INTO ORDERS VALUES(4,1,104,250);
INSERT INTO ORDERS VALUES(5,4,105,700);

-- Part B
-- 1. Customer name and city
SELECT NAME, C.ADDRESS.CITY AS CITY FROM CUSTOMER C;

-- 2. Restaurant-wise order count
SELECT R.NAME, COUNT(*) AS ORDER_COUNT
FROM RESTAURANT R JOIN ORDERS O ON R.RESTAURANT_ID = O.RESTAURANT_ID
GROUP BY R.NAME;

-- 3. Customer with maximum orders
SELECT * FROM (
  SELECT C.NAME, COUNT(*) AS TOTAL_ORDERS
  FROM CUSTOMER C JOIN ORDERS O ON C.CUSTOMER_ID = O.CUSTOMER_ID
  GROUP BY C.NAME
  ORDER BY TOTAL_ORDERS DESC
) WHERE ROWNUM = 1;

-- 4. Highest order amount
SELECT MAX(AMOUNT) AS HIGHEST_ORDER_AMOUNT FROM ORDERS;

-- 5. Backup table
CREATE TABLE ORDERS_BACKUP AS SELECT * FROM ORDERS;

-- 6. Explain (theory):
-- Replicating customer data among regional servers keeps the app responsive to
-- customers in any city and ensures orders can still be placed and tracked even
-- if one regional server experiences downtime.
```

---

## Question 8: Real Estate Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE OWNER(
  OWNER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE PROPERTY(
  PROPERTY_ID INT PRIMARY KEY,
  OWNER_ID INT REFERENCES OWNER(OWNER_ID),
  CITY VARCHAR2(30),
  PRICE NUMBER,
  STATUS VARCHAR2(20)
);

CREATE TABLE BUYER(
  BUYER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  PROPERTY_ID INT REFERENCES PROPERTY(PROPERTY_ID)
);

INSERT INTO OWNER VALUES(1,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'));
INSERT INTO OWNER VALUES(2,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'));
INSERT INTO OWNER VALUES(3,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'));
INSERT INTO OWNER VALUES(4,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'));
INSERT INTO OWNER VALUES(5,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'));

INSERT INTO PROPERTY VALUES(101,1,'Mumbai',9500000,'Available');
INSERT INTO PROPERTY VALUES(102,2,'Kolkata',4500000,'Sold');
INSERT INTO PROPERTY VALUES(103,3,'Pune',6200000,'Available');
INSERT INTO PROPERTY VALUES(104,4,'Delhi',12000000,'Available');
INSERT INTO PROPERTY VALUES(105,5,'Chennai',5300000,'Sold');

INSERT INTO BUYER VALUES(1,'Vikram Rao',102);
INSERT INTO BUYER VALUES(2,'Neha Joshi',105);
INSERT INTO BUYER VALUES(3,'Karan Mehta',NULL);
INSERT INTO BUYER VALUES(4,'Divya Nair',NULL);
INSERT INTO BUYER VALUES(5,'Arjun Singh',NULL);

-- Part B
-- 1. Owner name and city
SELECT NAME, O.ADDRESS.CITY AS CITY FROM OWNER O;

-- 2. Available properties
SELECT * FROM PROPERTY WHERE STATUS = 'Available';

-- 3. City-wise property count
SELECT CITY, COUNT(*) AS PROPERTY_COUNT FROM PROPERTY GROUP BY CITY;

-- 4. Most expensive property
SELECT * FROM (
  SELECT * FROM PROPERTY ORDER BY PRICE DESC
) WHERE ROWNUM = 1;

-- 5. Backup table
CREATE TABLE PROPERTY_BACKUP AS SELECT * FROM PROPERTY;

-- 6. Explain (theory):
-- Replicating property records to the corporate office ensures head-office
-- reporting and audits can proceed even if a regional/city office's database
-- becomes temporarily unavailable, and protects data against site-level failure.
```

---

## Question 9: Movie Ticket Booking System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE CUSTOMER(
  CUSTOMER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE MOVIE(
  MOVIE_ID INT PRIMARY KEY,
  TITLE VARCHAR2(50),
  TICKET_PRICE NUMBER
);

CREATE TABLE BOOKING(
  BOOKING_ID INT PRIMARY KEY,
  CUSTOMER_ID INT REFERENCES CUSTOMER(CUSTOMER_ID),
  MOVIE_ID INT REFERENCES MOVIE(MOVIE_ID),
  TICKETS INT,
  AMOUNT NUMBER
);

INSERT INTO CUSTOMER VALUES(1,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'));
INSERT INTO CUSTOMER VALUES(2,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'));
INSERT INTO CUSTOMER VALUES(3,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'));
INSERT INTO CUSTOMER VALUES(4,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'));
INSERT INTO CUSTOMER VALUES(5,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'));

INSERT INTO MOVIE VALUES(101,'Pathaan',250);
INSERT INTO MOVIE VALUES(102,'Jawan',280);
INSERT INTO MOVIE VALUES(103,'Animal',300);
INSERT INTO MOVIE VALUES(104,'Gadar 2',220);
INSERT INTO MOVIE VALUES(105,'Stree 2',260);

INSERT INTO BOOKING VALUES(1,1,101,2,500);
INSERT INTO BOOKING VALUES(2,2,102,3,840);
INSERT INTO BOOKING VALUES(3,3,101,4,1000);
INSERT INTO BOOKING VALUES(4,4,103,2,600);
INSERT INTO BOOKING VALUES(5,1,104,1,220);

-- Part B
-- 1. Customer name and city
SELECT NAME, C.ADDRESS.CITY AS CITY FROM CUSTOMER C;

-- 2. Movie-wise booking count
SELECT M.TITLE, COUNT(*) AS BOOKING_COUNT
FROM MOVIE M JOIN BOOKING B ON M.MOVIE_ID = B.MOVIE_ID
GROUP BY M.TITLE;

-- 3. Highest revenue-generating movie
SELECT * FROM (
  SELECT M.TITLE, SUM(B.AMOUNT) AS REVENUE
  FROM MOVIE M JOIN BOOKING B ON M.MOVIE_ID = B.MOVIE_ID
  GROUP BY M.TITLE
  ORDER BY REVENUE DESC
) WHERE ROWNUM = 1;

-- 4. Customer who booked maximum tickets
SELECT * FROM (
  SELECT C.NAME, SUM(B.TICKETS) AS TOTAL_TICKETS
  FROM CUSTOMER C JOIN BOOKING B ON C.CUSTOMER_ID = B.CUSTOMER_ID
  GROUP BY C.NAME
  ORDER BY TOTAL_TICKETS DESC
) WHERE ROWNUM = 1;

-- 5. Backup table
CREATE TABLE BOOKING_BACKUP AS SELECT * FROM BOOKING;

-- 6. Explain (theory):
-- Replicating booking data to a central server means that if one theatre's local
-- server crashes during a show, booking history and seat allocations are not lost
-- and can be restored from the replica, preventing double-booking or data loss.
```

---

## Question 10: Agriculture Produce Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE FARMER(
  FARMER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE CROP(
  CROP_ID INT PRIMARY KEY,
  CROP_NAME VARCHAR2(30),
  FARMER_ID INT REFERENCES FARMER(FARMER_ID)
);

CREATE TABLE SALES(
  SALE_ID INT PRIMARY KEY,
  CROP_ID INT REFERENCES CROP(CROP_ID),
  QUANTITY_KG NUMBER,
  AMOUNT NUMBER
);

INSERT INTO FARMER VALUES(1,'Ramesh Patil',ADDRESS_TYPE('Village Road','Nashik','422001'));
INSERT INTO FARMER VALUES(2,'Suresh Yadav',ADDRESS_TYPE('Main Road','Pune','411004'));
INSERT INTO FARMER VALUES(3,'Ganesh More',ADDRESS_TYPE('Farm Lane','Nagpur','440001'));
INSERT INTO FARMER VALUES(4,'Mahesh Deshmukh',ADDRESS_TYPE('Church Road','Kolhapur','416001'));
INSERT INTO FARMER VALUES(5,'Dinesh Jadhav',ADDRESS_TYPE('Market Road','Satara','415001'));

INSERT INTO CROP VALUES(101,'Wheat',1);
INSERT INTO CROP VALUES(102,'Rice',2);
INSERT INTO CROP VALUES(103,'Sugarcane',3);
INSERT INTO CROP VALUES(104,'Cotton',4);
INSERT INTO CROP VALUES(105,'Soybean',5);

INSERT INTO SALES VALUES(1,101,1200,24000);
INSERT INTO SALES VALUES(2,102,900,27000);
INSERT INTO SALES VALUES(3,103,2000,40000);
INSERT INTO SALES VALUES(4,104,800,32000);
INSERT INTO SALES VALUES(5,105,1500,45000);

-- Part B
-- 1. Farmer name and city
SELECT NAME, F.ADDRESS.CITY AS CITY FROM FARMER F;

-- 2. Crops sold above 1000 kg
SELECT C.CROP_NAME, S.QUANTITY_KG
FROM CROP C JOIN SALES S ON C.CROP_ID = S.CROP_ID
WHERE S.QUANTITY_KG > 1000;

-- 3. Farmer having maximum sales
SELECT * FROM (
  SELECT F.NAME, SUM(S.AMOUNT) AS TOTAL_SALES
  FROM FARMER F
  JOIN CROP C ON F.FARMER_ID = C.FARMER_ID
  JOIN SALES S ON C.CROP_ID = S.CROP_ID
  GROUP BY F.NAME
  ORDER BY TOTAL_SALES DESC
) WHERE ROWNUM = 1;

-- 4. Crop-wise total sales
SELECT C.CROP_NAME, SUM(S.AMOUNT) AS TOTAL_SALES
FROM CROP C JOIN SALES S ON C.CROP_ID = S.CROP_ID
GROUP BY C.CROP_NAME;

-- 5. Backup table
CREATE TABLE SALES_BACKUP AS SELECT * FROM SALES;

-- 6. Explain (theory):
-- Parallel processing lets the cooperative aggregate sales figures from many
-- collection centers at once (splitting work across processors), so seasonal
-- reports covering thousands of farmers and crops are generated quickly.
```

