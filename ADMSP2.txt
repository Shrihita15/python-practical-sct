---

## Question 11: Distributed Hospital Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE HOSPITAL(
  HOSPITAL_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  CITY VARCHAR2(30)
);

CREATE TABLE DOCTOR(
  DOCTOR_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  SPECIALIZATION VARCHAR2(30),
  HOSPITAL_ID INT REFERENCES HOSPITAL(HOSPITAL_ID)
);

CREATE TABLE PATIENT(
  PATIENT_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE,
  HOSPITAL_ID INT REFERENCES HOSPITAL(HOSPITAL_ID)
);

CREATE TABLE APPOINTMENT(
  APPOINTMENT_ID INT PRIMARY KEY,
  DOCTOR_ID INT REFERENCES DOCTOR(DOCTOR_ID),
  PATIENT_ID INT REFERENCES PATIENT(PATIENT_ID),
  APPOINTMENT_DATE DATE
);

INSERT INTO HOSPITAL VALUES(1,'City Care','Mumbai');
INSERT INTO HOSPITAL VALUES(2,'Sahyadri','Pune');
INSERT INTO HOSPITAL VALUES(3,'Wockhardt','Nashik');
INSERT INTO HOSPITAL VALUES(4,'Orange City Hospital','Nagpur');

INSERT INTO DOCTOR VALUES(1,'Dr. Mehta','Cardiology',1);
INSERT INTO DOCTOR VALUES(2,'Dr. Shah','Neurology',2);
INSERT INTO DOCTOR VALUES(3,'Dr. Joshi','Orthopedics',3);
INSERT INTO DOCTOR VALUES(4,'Dr. Patil','Dermatology',4);
INSERT INTO DOCTOR VALUES(5,'Dr. Rao','Cardiology',1);

INSERT INTO PATIENT VALUES(101,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'),1);
INSERT INTO PATIENT VALUES(102,'Anushka Sen',ADDRESS_TYPE('Park Street','Pune','411004'),2);
INSERT INTO PATIENT VALUES(103,'Salman Khan',ADDRESS_TYPE('F.C Road','Nashik','422001'),3);
INSERT INTO PATIENT VALUES(104,'Amir Khan',ADDRESS_TYPE('Civil Lines','Nagpur','440001'),4);
INSERT INTO PATIENT VALUES(105,'Samaira Khanna',ADDRESS_TYPE('Link Road','Mumbai','400050'),1);

INSERT INTO APPOINTMENT VALUES(1,1,101,DATE '2025-06-01');
INSERT INTO APPOINTMENT VALUES(2,2,102,DATE '2025-06-02');
INSERT INTO APPOINTMENT VALUES(3,3,103,DATE '2025-06-03');
INSERT INTO APPOINTMENT VALUES(4,1,105,DATE '2025-06-04');
INSERT INTO APPOINTMENT VALUES(5,4,104,DATE '2025-06-05');

-- Part B
-- 1. Complete patient details
SELECT P.PATIENT_ID, P.NAME, P.ADDRESS.STREET, P.ADDRESS.CITY, P.ADDRESS.PINCODE, P.HOSPITAL_ID
FROM PATIENT P;

-- 2. Patient name and city
SELECT NAME, P.ADDRESS.CITY AS CITY FROM PATIENT P;

-- 3. Doctor-wise appointment count
SELECT D.NAME, COUNT(*) AS APPOINTMENT_COUNT
FROM DOCTOR D JOIN APPOINTMENT A ON D.DOCTOR_ID = A.DOCTOR_ID
GROUP BY D.NAME;

-- 4. Hospital having maximum number of patients
SELECT * FROM (
  SELECT H.NAME, COUNT(*) AS PATIENT_COUNT
  FROM HOSPITAL H JOIN PATIENT P ON H.HOSPITAL_ID = P.HOSPITAL_ID
  GROUP BY H.NAME
  ORDER BY PATIENT_COUNT DESC
) WHERE ROWNUM = 1;

-- 5. Backup of Patient table
CREATE TABLE PATIENT_BACKUP AS SELECT * FROM PATIENT;

-- 6. Advantage of data replication:
-- Even if one hospital's local server fails, patient history remains accessible
-- from the central replica, ensuring continuity of care.
```

---

## Question 12: Distributed University Examination System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE STUDENT(
  STUDENT_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE COURSE(
  COURSE_ID INT PRIMARY KEY,
  COURSE_NAME VARCHAR2(50)
);

CREATE TABLE ENROLLMENT(
  ENROLLMENT_ID INT PRIMARY KEY,
  STUDENT_ID INT REFERENCES STUDENT(STUDENT_ID),
  COURSE_ID INT REFERENCES COURSE(COURSE_ID),
  MARKS NUMBER
);

INSERT INTO STUDENT VALUES(1,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'));
INSERT INTO STUDENT VALUES(2,'Anushka Sen',ADDRESS_TYPE('Park Street','Pune','411004'));
INSERT INTO STUDENT VALUES(3,'Salman Khan',ADDRESS_TYPE('F.C Road','Nashik','422001'));
INSERT INTO STUDENT VALUES(4,'Amir Khan',ADDRESS_TYPE('Civil Lines','Mumbai','400002'));
INSERT INTO STUDENT VALUES(5,'Samaira Khanna',ADDRESS_TYPE('Link Road','Pune','411005'));

INSERT INTO COURSE VALUES(101,'DBMS');
INSERT INTO COURSE VALUES(102,'Data Structures');
INSERT INTO COURSE VALUES(103,'Operating Systems');

INSERT INTO ENROLLMENT VALUES(1,1,101,85);
INSERT INTO ENROLLMENT VALUES(2,2,101,90);
INSERT INTO ENROLLMENT VALUES(3,3,102,78);
INSERT INTO ENROLLMENT VALUES(4,4,102,88);
INSERT INTO ENROLLMENT VALUES(5,5,103,95);

-- Part B
-- 1. Student name and city
SELECT NAME, S.ADDRESS.CITY AS CITY FROM STUDENT S;

-- 2. Students scoring more than 80 marks
SELECT S.NAME, E.MARKS
FROM STUDENT S JOIN ENROLLMENT E ON S.STUDENT_ID = E.STUDENT_ID
WHERE E.MARKS > 80;

-- 3. Average marks of each course
SELECT C.COURSE_NAME, AVG(E.MARKS) AS AVG_MARKS
FROM COURSE C JOIN ENROLLMENT E ON C.COURSE_ID = E.COURSE_ID
GROUP BY C.COURSE_NAME;

-- 4. Topper of each course
SELECT COURSE_ID, NAME, MARKS FROM (
  SELECT E.COURSE_ID, S.NAME, E.MARKS,
         RANK() OVER (PARTITION BY E.COURSE_ID ORDER BY E.MARKS DESC) AS RNK
  FROM STUDENT S JOIN ENROLLMENT E ON S.STUDENT_ID = E.STUDENT_ID
)
WHERE RNK = 1;

-- 5. Backup table of Enrollment
CREATE TABLE ENROLLMENT_BACKUP AS SELECT * FROM ENROLLMENT;

-- 6. Explain (theory):
-- Parallel query execution splits large aggregation/sorting operations (like
-- ranking every student across all campuses) across multiple processors so
-- university-wide result reports are generated much faster than sequential execution.
```

---

## Question 13: Banking Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE BRANCH(
  BRANCH_ID INT PRIMARY KEY,
  BRANCH_NAME VARCHAR2(50),
  CITY VARCHAR2(30)
);

CREATE TABLE CUSTOMER(
  CUSTOMER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE,
  BRANCH_ID INT REFERENCES BRANCH(BRANCH_ID)
);

CREATE TABLE ACCOUNT(
  ACCOUNT_NO INT PRIMARY KEY,
  CUSTOMER_ID INT REFERENCES CUSTOMER(CUSTOMER_ID),
  BALANCE NUMBER
);

INSERT INTO BRANCH VALUES(1,'Andheri Branch','Mumbai');
INSERT INTO BRANCH VALUES(2,'Koregaon Branch','Pune');
INSERT INTO BRANCH VALUES(3,'Salt Lake Branch','Kolkata');

INSERT INTO CUSTOMER VALUES(1,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'),1);
INSERT INTO CUSTOMER VALUES(2,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'),3);
INSERT INTO CUSTOMER VALUES(3,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'),2);
INSERT INTO CUSTOMER VALUES(4,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'),1);
INSERT INTO CUSTOMER VALUES(5,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'),2);

INSERT INTO ACCOUNT VALUES(1001,1,75000);
INSERT INTO ACCOUNT VALUES(1002,2,120000);
INSERT INTO ACCOUNT VALUES(1003,3,25000);
INSERT INTO ACCOUNT VALUES(1004,4,60000);
INSERT INTO ACCOUNT VALUES(1005,5,45000);

-- Part B
-- 1. Customer name and city
SELECT NAME, C.ADDRESS.CITY AS CITY FROM CUSTOMER C;

-- 2. Customers with balance greater than 50,000
SELECT C.NAME, A.BALANCE
FROM CUSTOMER C JOIN ACCOUNT A ON C.CUSTOMER_ID = A.CUSTOMER_ID
WHERE A.BALANCE > 50000;

-- 3. Branch with highest total deposits
SELECT * FROM (
  SELECT B.BRANCH_NAME, SUM(A.BALANCE) AS TOTAL_DEPOSITS
  FROM BRANCH B
  JOIN CUSTOMER C ON B.BRANCH_ID = C.BRANCH_ID
  JOIN ACCOUNT A ON C.CUSTOMER_ID = A.CUSTOMER_ID
  GROUP BY B.BRANCH_NAME
  ORDER BY TOTAL_DEPOSITS DESC
) WHERE ROWNUM = 1;

-- 4. Customer with highest balance
SELECT * FROM (
  SELECT C.NAME, A.BALANCE
  FROM CUSTOMER C JOIN ACCOUNT A ON C.CUSTOMER_ID = A.CUSTOMER_ID
  ORDER BY A.BALANCE DESC
) WHERE ROWNUM = 1;

-- 5. Backup of Account table
CREATE TABLE ACCOUNT_BACKUP AS SELECT * FROM ACCOUNT;

-- 6. Explain (theory):
-- Synchronous replication writes to the primary and replica at the same time
-- (before the transaction is confirmed), guaranteeing zero data loss but adding
-- latency. Asynchronous replication commits at the primary first and copies to
-- the replica afterward, giving faster response times but a small risk of losing
-- the most recent transactions if the primary fails before the copy completes.
```

---

## Question 14: Online Shopping System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE CUSTOMER(
  CUSTOMER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE PRODUCT(
  PRODUCT_ID INT PRIMARY KEY,
  PRODUCT_NAME VARCHAR2(50),
  PRICE NUMBER
);

CREATE TABLE ORDERS(
  ORDER_ID INT PRIMARY KEY,
  CUSTOMER_ID INT REFERENCES CUSTOMER(CUSTOMER_ID),
  PRODUCT_ID INT REFERENCES PRODUCT(PRODUCT_ID),
  QUANTITY INT,
  AMOUNT NUMBER
);

INSERT INTO CUSTOMER VALUES(1,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'));
INSERT INTO CUSTOMER VALUES(2,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'));
INSERT INTO CUSTOMER VALUES(3,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'));
INSERT INTO CUSTOMER VALUES(4,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'));
INSERT INTO CUSTOMER VALUES(5,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'));

INSERT INTO PRODUCT VALUES(101,'Laptop',55000);
INSERT INTO PRODUCT VALUES(102,'Smartphone',22000);
INSERT INTO PRODUCT VALUES(103,'Headphones',3000);
INSERT INTO PRODUCT VALUES(104,'Smartwatch',8000);
INSERT INTO PRODUCT VALUES(105,'Tablet',18000);

INSERT INTO ORDERS VALUES(1,1,101,1,55000);
INSERT INTO ORDERS VALUES(2,2,102,2,44000);
INSERT INTO ORDERS VALUES(3,3,103,3,9000);
INSERT INTO ORDERS VALUES(4,4,104,1,8000);
INSERT INTO ORDERS VALUES(5,1,105,1,18000);

-- Part B
-- 1. Customer name and city
SELECT NAME, C.ADDRESS.CITY AS CITY FROM CUSTOMER C;

-- 2. Products costing more than 5,000
SELECT PRODUCT_NAME, PRICE FROM PRODUCT WHERE PRICE > 5000;

-- 3. Total purchase amount for each customer
SELECT C.NAME, SUM(O.AMOUNT) AS TOTAL_PURCHASE
FROM CUSTOMER C JOIN ORDERS O ON C.CUSTOMER_ID = O.CUSTOMER_ID
GROUP BY C.NAME;

-- 4. Customer with highest purchase amount
SELECT * FROM (
  SELECT C.NAME, SUM(O.AMOUNT) AS TOTAL_PURCHASE
  FROM CUSTOMER C JOIN ORDERS O ON C.CUSTOMER_ID = O.CUSTOMER_ID
  GROUP BY C.NAME
  ORDER BY TOTAL_PURCHASE DESC
) WHERE ROWNUM = 1;

-- 5. Backup table
CREATE TABLE PRODUCT_BACKUP AS SELECT * FROM PRODUCT;

-- 6. Explain (theory):
-- Replication in e-commerce keeps customer and order data available during
-- flash sales / peak traffic, and lets regional servers serve requests locally
-- with low latency while staying in sync with the central database.
```

---

## Question 15: Library Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE BOOK(
  BOOK_ID INT PRIMARY KEY,
  TITLE VARCHAR2(50),
  AUTHOR VARCHAR2(50)
);

CREATE TABLE MEMBER(
  MEMBER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE ISSUE(
  ISSUE_ID INT PRIMARY KEY,
  BOOK_ID INT REFERENCES BOOK(BOOK_ID),
  MEMBER_ID INT REFERENCES MEMBER(MEMBER_ID),
  ISSUE_DATE DATE,
  DUE_DATE DATE,
  RETURN_DATE DATE
);

INSERT INTO BOOK VALUES(101,'Database Systems','Silberschatz');
INSERT INTO BOOK VALUES(102,'Operating Systems','Galvin');
INSERT INTO BOOK VALUES(103,'Computer Networks','Tanenbaum');
INSERT INTO BOOK VALUES(104,'Data Structures','Aho');
INSERT INTO BOOK VALUES(105,'Software Engineering','Pressman');

INSERT INTO MEMBER VALUES(1,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'));
INSERT INTO MEMBER VALUES(2,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'));
INSERT INTO MEMBER VALUES(3,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'));
INSERT INTO MEMBER VALUES(4,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'));
INSERT INTO MEMBER VALUES(5,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'));

INSERT INTO ISSUE VALUES(1,101,1,DATE '2025-01-01',DATE '2025-01-15',NULL);
INSERT INTO ISSUE VALUES(2,102,2,DATE '2025-01-05',DATE '2025-01-19',DATE '2025-01-18');
INSERT INTO ISSUE VALUES(3,103,1,DATE '2025-01-10',DATE '2025-01-24',NULL);
INSERT INTO ISSUE VALUES(4,104,3,DATE '2025-01-12',DATE '2025-01-26',DATE '2025-01-25');
INSERT INTO ISSUE VALUES(5,105,1,DATE '2025-01-15',DATE '2025-01-29',NULL);

-- Part B
-- 1. Member name and city
SELECT NAME, M.ADDRESS.CITY AS CITY FROM MEMBER M;

-- 2. Overdue books (not returned and past due date)
SELECT B.TITLE, I.DUE_DATE
FROM BOOK B JOIN ISSUE I ON B.BOOK_ID = I.BOOK_ID
WHERE I.RETURN_DATE IS NULL AND I.DUE_DATE < SYSDATE;

-- 3. Member who has issued maximum number of books
SELECT * FROM (
  SELECT M.NAME, COUNT(*) AS BOOKS_ISSUED
  FROM MEMBER M JOIN ISSUE I ON M.MEMBER_ID = I.MEMBER_ID
  GROUP BY M.NAME
  ORDER BY BOOKS_ISSUED DESC
) WHERE ROWNUM = 1;

-- 4. Total number of books issued
SELECT COUNT(*) AS TOTAL_ISSUED FROM ISSUE;

-- 5. Backup of Book table
CREATE TABLE BOOK_BACKUP AS SELECT * FROM BOOK;

-- 6. Explain (theory):
-- Parallel processing allows the library system to scan and aggregate issue
-- records from all campuses simultaneously, so overdue lists and usage reports
-- across the whole university are produced quickly instead of one campus at a time.
```

---

## Question 16: Airline Reservation System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE PASSENGER(
  PASSENGER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE FLIGHT(
  FLIGHT_NO INT PRIMARY KEY,
  FLIGHT_NAME VARCHAR2(50),
  SOURCE VARCHAR2(30),
  DESTINATION VARCHAR2(30)
);

CREATE TABLE RESERVATION(
  PNR_NO INT PRIMARY KEY,
  PASSENGER_ID INT REFERENCES PASSENGER(PASSENGER_ID),
  FLIGHT_NO INT REFERENCES FLIGHT(FLIGHT_NO),
  SEAT_NO INT,
  STATUS VARCHAR2(20)
);

INSERT INTO PASSENGER VALUES(1,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'));
INSERT INTO PASSENGER VALUES(2,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'));
INSERT INTO PASSENGER VALUES(3,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'));
INSERT INTO PASSENGER VALUES(4,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'));
INSERT INTO PASSENGER VALUES(5,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'));

INSERT INTO FLIGHT VALUES(6001,'IndiGo 101','Mumbai','Delhi');
INSERT INTO FLIGHT VALUES(6002,'Air India 202','Delhi','Chennai');
INSERT INTO FLIGHT VALUES(6003,'SpiceJet 303','Pune','Delhi');
INSERT INTO FLIGHT VALUES(6004,'Vistara 404','Kolkata','Mumbai');
INSERT INTO FLIGHT VALUES(6005,'GoAir 505','Chennai','Delhi');

INSERT INTO RESERVATION VALUES(9001,1,6001,12,'Confirmed');
INSERT INTO RESERVATION VALUES(9002,2,6004,7,'Confirmed');
INSERT INTO RESERVATION VALUES(9003,3,6003,15,'Confirmed');
INSERT INTO RESERVATION VALUES(9004,4,6001,20,'Waiting');
INSERT INTO RESERVATION VALUES(9005,5,6005,5,'Confirmed');

-- Part B
-- 1. Passenger name and city
SELECT NAME, P.ADDRESS.CITY AS CITY FROM PASSENGER P;

-- 2. Passengers traveling to Delhi
SELECT DISTINCT P.NAME, F.DESTINATION
FROM PASSENGER P
JOIN RESERVATION R ON P.PASSENGER_ID = R.PASSENGER_ID
JOIN FLIGHT F ON R.FLIGHT_NO = F.FLIGHT_NO
WHERE F.DESTINATION = 'Delhi';

-- 3. Flight with maximum reservations
SELECT * FROM (
  SELECT F.FLIGHT_NAME, COUNT(*) AS TOTAL_RESERVATIONS
  FROM FLIGHT F JOIN RESERVATION R ON F.FLIGHT_NO = R.FLIGHT_NO
  GROUP BY F.FLIGHT_NAME
  ORDER BY TOTAL_RESERVATIONS DESC
) WHERE ROWNUM = 1;

-- 4. Destination-wise passenger count
SELECT F.DESTINATION, COUNT(*) AS PASSENGER_COUNT
FROM FLIGHT F JOIN RESERVATION R ON F.FLIGHT_NO = R.FLIGHT_NO
GROUP BY F.DESTINATION;

-- 5. Backup table
CREATE TABLE RESERVATION_BACKUP AS SELECT * FROM RESERVATION;

-- 6. Explain (theory):
-- Replicating reservation data to a central server means that if one airport's
-- local server fails, booking/seat data can be recovered from the replica so
-- passengers are not double-booked or lose their reservations.
```

---

## Question 17: Smart City Electricity Billing System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE CONSUMER(
  CONSUMER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE,
  ZONE VARCHAR2(20)
);

CREATE TABLE METER(
  METER_NO INT PRIMARY KEY,
  CONSUMER_ID INT REFERENCES CONSUMER(CONSUMER_ID),
  METER_TYPE VARCHAR2(20)
);

CREATE TABLE BILL(
  BILL_ID INT PRIMARY KEY,
  CONSUMER_ID INT REFERENCES CONSUMER(CONSUMER_ID),
  AMOUNT NUMBER,
  STATUS VARCHAR2(20)
);

INSERT INTO CONSUMER VALUES(1,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'),'Zone A');
INSERT INTO CONSUMER VALUES(2,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'),'Zone B');
INSERT INTO CONSUMER VALUES(3,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'),'Zone A');
INSERT INTO CONSUMER VALUES(4,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'),'Zone C');
INSERT INTO CONSUMER VALUES(5,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'),'Zone B');

INSERT INTO METER VALUES(501,1,'Digital');
INSERT INTO METER VALUES(502,2,'Digital');
INSERT INTO METER VALUES(503,3,'Analog');
INSERT INTO METER VALUES(504,4,'Digital');
INSERT INTO METER VALUES(505,5,'Analog');

INSERT INTO BILL VALUES(1,1,1500,'Unpaid');
INSERT INTO BILL VALUES(2,2,2200,'Paid');
INSERT INTO BILL VALUES(3,3,900,'Unpaid');
INSERT INTO BILL VALUES(4,4,3000,'Paid');
INSERT INTO BILL VALUES(5,5,1800,'Unpaid');

-- Part B
-- 1. Consumer name and city
SELECT NAME, C.ADDRESS.CITY AS CITY FROM CONSUMER C;

-- 2. Unpaid bills
SELECT * FROM BILL WHERE STATUS = 'Unpaid';

-- 3. Consumer with highest bill amount
SELECT * FROM (
  SELECT C.NAME, B.AMOUNT
  FROM CONSUMER C JOIN BILL B ON C.CONSUMER_ID = B.CONSUMER_ID
  ORDER BY B.AMOUNT DESC
) WHERE ROWNUM = 1;

-- 4. Zone-wise bill collection
SELECT C.ZONE, SUM(B.AMOUNT) AS TOTAL_COLLECTION
FROM CONSUMER C JOIN BILL B ON C.CONSUMER_ID = B.CONSUMER_ID
WHERE B.STATUS = 'Paid'
GROUP BY C.ZONE;

-- 5. Backup table
CREATE TABLE BILL_BACKUP AS SELECT * FROM BILL;

-- 6. Explain (theory):
-- Replicating consumer/billing data from zonal databases to a central server
-- ensures billing history is not lost if a zonal server fails, supporting
-- continuity of billing operations during disaster recovery.
```

---

## Question 18: Hotel Reservation System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE GUEST(
  GUEST_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE ROOM(
  ROOM_NO INT PRIMARY KEY,
  ROOM_TYPE VARCHAR2(20),
  PRICE NUMBER,
  STATUS VARCHAR2(20)
);

CREATE TABLE BOOKING(
  BOOKING_ID INT PRIMARY KEY,
  GUEST_ID INT REFERENCES GUEST(GUEST_ID),
  ROOM_NO INT REFERENCES ROOM(ROOM_NO),
  CHECKIN_DATE DATE,
  CHECKOUT_DATE DATE,
  AMOUNT NUMBER
);

INSERT INTO GUEST VALUES(1,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'));
INSERT INTO GUEST VALUES(2,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'));
INSERT INTO GUEST VALUES(3,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'));
INSERT INTO GUEST VALUES(4,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'));
INSERT INTO GUEST VALUES(5,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'));

INSERT INTO ROOM VALUES(101,'Deluxe',5000,'Occupied');
INSERT INTO ROOM VALUES(102,'Suite',9000,'Available');
INSERT INTO ROOM VALUES(103,'Standard',3000,'Occupied');
INSERT INTO ROOM VALUES(104,'Deluxe',5000,'Available');
INSERT INTO ROOM VALUES(105,'Suite',9000,'Occupied');

INSERT INTO BOOKING VALUES(1,1,101,DATE '2025-06-01',DATE '2025-06-03',10000);
INSERT INTO BOOKING VALUES(2,2,103,DATE '2025-06-02',DATE '2025-06-04',6000);
INSERT INTO BOOKING VALUES(3,3,105,DATE '2025-06-05',DATE '2025-06-08',27000);
INSERT INTO BOOKING VALUES(4,4,101,DATE '2025-06-10',DATE '2025-06-11',5000);
INSERT INTO BOOKING VALUES(5,5,103,DATE '2025-06-12',DATE '2025-06-13',3000);

-- Part B
-- 1. Guest name and city
SELECT NAME, G.ADDRESS.CITY AS CITY FROM GUEST G;

-- 2. Room occupancy details
SELECT ROOM_NO, ROOM_TYPE, STATUS FROM ROOM;

-- 3. Highest booking amount
SELECT MAX(AMOUNT) AS HIGHEST_BOOKING_AMOUNT FROM BOOKING;

-- 4. Room-wise booking count
SELECT ROOM_NO, COUNT(*) AS BOOKING_COUNT FROM BOOKING GROUP BY ROOM_NO;

-- 5. Backup table
CREATE TABLE BOOKING_BACKUP AS SELECT * FROM BOOKING;

-- 6. Explain (theory):
-- Parallel transaction processing lets multiple bookings/check-ins across
-- different regional hotel servers be processed simultaneously without
-- blocking each other, improving throughput during peak booking periods.
```

---

## Question 19: Vehicle Registration System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE OWNER(
  OWNER_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE
);

CREATE TABLE VEHICLE(
  VEHICLE_NO VARCHAR2(15) PRIMARY KEY,
  OWNER_ID INT REFERENCES OWNER(OWNER_ID),
  MODEL VARCHAR2(30),
  PRICE NUMBER,
  REG_DATE DATE
);

INSERT INTO OWNER VALUES(1,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'));
INSERT INTO OWNER VALUES(2,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'));
INSERT INTO OWNER VALUES(3,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'));
INSERT INTO OWNER VALUES(4,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'));
INSERT INTO OWNER VALUES(5,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'));

INSERT INTO VEHICLE VALUES('MH12AB1234',1,'Honda City',1200000,DATE '2023-05-10');
INSERT INTO VEHICLE VALUES('WB02CD5678',2,'Maruti Swift',700000,DATE '2021-08-15');
INSERT INTO VEHICLE VALUES('MH14EF9012',3,'Hyundai Creta',1500000,DATE '2024-01-20');
INSERT INTO VEHICLE VALUES('DL03GH3456',4,'Tata Nexon',1100000,DATE '2022-11-05');
INSERT INTO VEHICLE VALUES('TN05IJ7890',5,'Mahindra XUV700',1900000,DATE '2023-09-25');

-- Part B
-- 1. Owner name and city
SELECT NAME, O.ADDRESS.CITY AS CITY FROM OWNER O;

-- 2. Vehicles registered after 2022
SELECT VEHICLE_NO, MODEL, REG_DATE FROM VEHICLE
WHERE REG_DATE > DATE '2022-12-31';

-- 3. Costliest vehicle
SELECT * FROM (
  SELECT * FROM VEHICLE ORDER BY PRICE DESC
) WHERE ROWNUM = 1;

-- 4. City-wise vehicle registrations
SELECT O.ADDRESS.CITY AS CITY, COUNT(*) AS TOTAL_VEHICLES
FROM OWNER O JOIN VEHICLE V ON O.OWNER_ID = V.OWNER_ID
GROUP BY O.ADDRESS.CITY;

-- 5. Backup table
CREATE TABLE VEHICLE_BACKUP AS SELECT * FROM VEHICLE;

-- 6. Explain (theory):
-- Replicating registration records to regional servers lets any RTO office
-- verify a vehicle's ownership and history instantly, even if the vehicle
-- was originally registered in a different region whose server is unreachable.
```

---

## Question 20: Corporate Employee Management System

```sql
CREATE TYPE ADDRESS_TYPE AS OBJECT(
  STREET VARCHAR2(50),
  CITY VARCHAR2(50),
  PINCODE VARCHAR2(10)
);

CREATE TABLE DEPARTMENT(
  DEPT_ID INT PRIMARY KEY,
  DEPT_NAME VARCHAR2(50)
);

CREATE TABLE EMPLOYEE(
  EMP_ID INT PRIMARY KEY,
  NAME VARCHAR2(50),
  ADDRESS ADDRESS_TYPE,
  DEPT_ID INT REFERENCES DEPARTMENT(DEPT_ID),
  SALARY NUMBER
);

INSERT INTO DEPARTMENT VALUES(1,'Human Resources');
INSERT INTO DEPARTMENT VALUES(2,'Finance');
INSERT INTO DEPARTMENT VALUES(3,'IT');

INSERT INTO EMPLOYEE VALUES(101,'Rahul Sharma',ADDRESS_TYPE('M.G Road','Mumbai','400001'),3,75000);
INSERT INTO EMPLOYEE VALUES(102,'Anushka Sen',ADDRESS_TYPE('Park Street','Kolkata','700016'),1,55000);
INSERT INTO EMPLOYEE VALUES(103,'Salman Khan',ADDRESS_TYPE('F.C Road','Pune','411004'),2,68000);
INSERT INTO EMPLOYEE VALUES(104,'Amir Khan',ADDRESS_TYPE('Sarojini Nagar','Delhi','110001'),3,92000);
INSERT INTO EMPLOYEE VALUES(105,'Samaira Khanna',ADDRESS_TYPE('Anna Nagar','Chennai','600040'),2,58000);

-- Part B
-- 1. Employee name and city
SELECT NAME, E.ADDRESS.CITY AS CITY FROM EMPLOYEE E;

-- 2. Employees earning more than 60,000
SELECT NAME, SALARY FROM EMPLOYEE WHERE SALARY > 60000;

-- 3. Average salary department-wise
SELECT D.DEPT_NAME, AVG(E.SALARY) AS AVG_SALARY
FROM DEPARTMENT D JOIN EMPLOYEE E ON D.DEPT_ID = E.DEPT_ID
GROUP BY D.DEPT_NAME;

-- 4. Highest-paid employee
SELECT * FROM (
  SELECT * FROM EMPLOYEE ORDER BY SALARY DESC
) WHERE ROWNUM = 1;

-- 5. Backup table
CREATE TABLE EMPLOYEE_BACKUP AS SELECT * FROM EMPLOYEE;

-- 6. Explain (theory):
-- Data replication lets each branch office's HR data remain available for
-- payroll processing even if a regional data center goes offline, while
-- parallel processing lets the multinational compute salary reports and
-- averages across thousands of employees in many branches simultaneously
-- rather than one record at a time.
```